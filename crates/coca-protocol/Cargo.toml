[package]
name = "coca-protocol"
version = "0.1.0"
edition = "2021"
description = "JSON-RPC wire protocol types for coca frontends and core."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
